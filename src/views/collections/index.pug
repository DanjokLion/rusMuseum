extends ../layout

block content
  h1.col-12.text-center.mb-5.mt-5.pt-5 Коллекции
  .container.min-h
    .row
      .col-12
        p.opis Ознакомьтесь с лучшими предметами музея разных направлений. На данный момент коллекция музея насчитывает более 2000 предметов и регулярно пополняется. На данной странице представлены экспонаты основных коллекций.
      if collections != null
        for item in collections
          .col-4.mb-3
            .card.text-center
              a.card-item-bg(href="/collections/"+item.link style="background-image: url('/uploads/collections/"+ ( item.thumbnail != null ? item.thumbnail : "noimage.png" ) + "')")
              .card-body
                h5.card-title.pb-3=item.title
                a.btn.btn-primary.mb-4(href="/collections/"+item.link) Перейти
        else
          h2.col-12.text-center Коллекций нет
